const User = require('./user');
const BlogPost = require('./blogpost');
const Comment = require('./comment');

// You can also include associations here if needed

module.exports = { User, BlogPost, Comment };

